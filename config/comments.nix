{
  #plugins.kommentary.enable = true; # no commentary. in nixvixm
  #plugins.ts-comments.enable = true; # allows changing comment chars per language
  # treesitter already does comment with gc / gcc, so I'm happy
}
